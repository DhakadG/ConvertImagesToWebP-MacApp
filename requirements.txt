# WebP Studio 2.0

# Required
customtkinter>=5.2.2
# 11.3 first shipped native AVIF, but everything below 12.3.0 carries HIGH
# advisories in ImageCms and crop/paste (GHSA-9hw9-ch79-4vh6,
# GHSA-6r8x-57c9-28j4) — both of which this app calls on every image.
Pillow>=12.3.0

# Optional, each degrades gracefully if absent (see: python main.py --check)
tkinterdnd2>=0.4.2      # drag & drop onto the window
piexif>=1.1.3           # strip GPS while keeping the rest of the EXIF
pillow-heif>=0.16.0     # read iPhone .heic / .heif

# Build only (macOS)
# py2app>=0.28.8
